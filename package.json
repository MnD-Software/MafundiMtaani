{
  "name": "mafundi-mtaani-platform",
  "private": true,
  "scripts": {
    "dev": "npm --prefix apps/web run dev",
    "build": "npm --prefix apps/web run build",
    "lint": "npm --prefix apps/web run lint",
    "api": "python -m uvicorn app.main:app --reload --app-dir services/api"
  }
}
